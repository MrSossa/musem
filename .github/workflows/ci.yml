name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  ci:
    name: ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Placeholder: añade aquí lint / tests del proyecto.
      - name: Check
        run: echo "sin checks configurados todavía"
